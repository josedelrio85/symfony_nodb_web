apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-pakcage.fullname" . }}
  labels:
    {{- include "helm-pakcage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-pakcage.selectorLabels" . | nindent 4 }}
