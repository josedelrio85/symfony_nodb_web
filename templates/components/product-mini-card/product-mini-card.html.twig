<div class="card product-mini-card text-left">
  <div class="card-body">
    <p class="h4 text-primary text-header">{{ top | trans }}</p>
    <p class="h5 text-primary font-weight-bold">{{ middle }} {{ bottom }}</p>
  </div>
</div>