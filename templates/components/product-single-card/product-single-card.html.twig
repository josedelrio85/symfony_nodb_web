<div class="card product-single-card mx-4">
  <div class="card-body d-flex flex-column justify-content-between p-0">
    <div class="card-body-top-part">
      <p class="h5 text-primary d-flex justify-content-center price">
        <span class="price-from mt-2">Desde</span>
        <span class="price-num mx-2 text-header">10</span>
        <span class="euro-symbol mt-3"> €</span><span class="period mt-3">/mes</span>
      </p>
    </div>
    <div class="p-4">
      <ul class="list-group list-group-flush bullet-list">
        {# {% for item in bullet_list_items %} #}
          <li class="list-group-item px-0">
            <span class="list-item-header h4 text-primary font-weight-bold">2 meses gratis</span><br>
            <span class="list-item-decription text-dark">con Dental</span>
          </li>
          <li class="list-group-item px-0">
            <span class="list-item-header h4 text-primary font-weight-bold">10% de descuento</span><br>
            <span class="list-item-decription text-dark">a partir de 4 asegurados</span>
          </li>
        {# {% endfor %} #}
      </ul>
      <button type="button" class="btn btn-primary btn-lg btn-block my-4 font-weight-bold">Contratar</button>
    </div>
  </div>
</div>


{# <div class="card product-single-card h-100">
  <div class="card-body d-flex flex-column justify-content-between">
  <div>
    <h2 class="h1 text-primary text-header">{{ item.title | trans}}</h2>
    <p class="h5 text-primary d-flex price">
      <span class="price-from mt-2">{{ item.price.pre | trans}}</span>
      <span class="price-num mx-2 text-header">{{ item.price.quantity | trans}}</span>
      <span class="euro-symbol"> {{ item.price.currency | trans}}</span>{{ item.price.period | trans}}
    </p>
    {% include 'components/simple-divider-line/simple-divider-line.html.twig' with {margin_y: 3, padding_x: 0} %}
    {% include 'components/bullet-list/bullet-list.html.twig' with { bullet_list_items: item.bullets } %}
  </div>
  <div class="d-flex flex-column">
    <button type="button" class=" btn btn-primary btn-lg btn-block mt-4 font-weight-bold">{{ item.cta.text | trans}}</button>
    <a href="{{ item.more_info.path | trans }}"><p class="text-center py-4 font-weight-bold h5">{{ item.more_info.text | trans}}</p></a>
  </div>
  </div>
</div> #}