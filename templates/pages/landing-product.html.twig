{% extends 'base.html.twig' %}

{% block title %}{{ "#{landing}.home.title" | trans }}{% endblock %}

{% block meta_description %}<meta name="description" content="{{ "#{landing}.home.meta_description" | trans }}">{% endblock %}

{% block body %}

  {# Hero product component #}
  {% include 'components/header-image/header-image.html.twig' with {
    image: "#{landing}.hero.image" | trans
  } %}

  {# SupertitleTitleSubtitle component #}
  {% include 'components/header-titles/header-titles.html.twig' with {
    mini_header: "#{landing}.minicards.suptitle" | trans,
    text_header: "#{landing}.minicards.title" | trans,
    content_header: "#{landing}.minicards.desc" | trans
  } %}

  {#TODO move this code to a component #}
  <div class="container mb-5">
    <div class="row">
      {% for card in cards %}
        <div class="col-6">
            {% include 'components/product-mini-card/product-mini-card.html.twig' with {
              title: card.top,
              subtitle: card.middle,
              body: card.bottom,
            } %}
        </div>
        {% endfor %}
    </div>
  </div>

  {# SupertitleTitleSubtitle component #}
  {% include 'components/header-titles/header-titles.html.twig' with {
    mini_header: "#{landing}.product_bullet.suptitle" | trans,
    text_header: "#{landing}.product_bullet.title" | trans,
    content_header: "#{landing}.product_bullet.desc" | trans
  } %}

  {# Product card component #}
  <div class="container mb-5">
    <div class="row">
      {% for item in product_bullets %}
        <div class="col-12 col-md-6 mb-4">
          {% include 'components/product-card/product-card.html.twig' with { product: item } %}
        </div>
      {% endfor %}
    </div>
  </div>

  {# Selector component #}
  {# <div class="container mb-5">
    <div class="row">
      <div class="col-12">
        {% include 'components/header-titles/header-titles.html.twig' with {
          mini_header: 'DESTACADO',
          text_header: 'Consulta nuestro cuadro médico',
          content_header: ''
        } %}
        <select class="form-control form-control-lg mt-3">
          <option>Selecciona provincia</option>
          <option>A Coruña</option>
          <option>Lugo</option>
          <option>Ourense</option>
          <option>Pontevedra</option>
        </select>
      </div>
    </div>
  </div> #}

  {# Simple divider line component #}
  {% include 'components/simple-divider-line/simple-divider-line.html.twig' with {margin_y: 5, padding_x: 3} %}

  {# SupertitleTitleSubtitle component #}
  {% include 'components/header-titles/header-titles.html.twig' with {
    mini_header: '',
    text_header: "#{landing}.desc.title" | trans,
    content_header: "#{landing}.desc.text" | trans
  } %}

  {# SupertitleTitleSubtitle component #}
  {# <div class="container-fluid bg-grey px-0">
    {% include 'components/header-titles/header-titles.html.twig' with {
      mini_header: "#{landing}.faq.suptitle" | trans,
      text_header:  "#{landing}.faq.title" | trans,
      content_header: ''
    } %}
  </div> #}

  {# Dropdown component #}
  {# {% include 'components/dropdown/dropdown.html.twig' with {
    dropdown_items: dropdown
  } %} #}

{% endblock %}
