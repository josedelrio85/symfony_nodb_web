{% extends 'base.html.twig' %}

{% block title %}{{ "home.title_detail" | trans }}{% endblock %}

{% block meta_description %}<meta name="description" content="{{ "home.meta_description" | trans }}">{% endblock %}

{% block body %}

  {# <h1>{{ "header.text1" | trans | raw }}</h1>
  <h2>{{ "header.text2" | trans | raw }}</h2>
  <h3>{{ "header.text3" | trans | raw }}</h3>
  <h4>{{ "header.text4" | trans | raw }}</h4>

  <h1>{{ "test.test1" | trans | raw }}</h1>
  <h2>{{ "test.test2" | trans | raw }}</h2> #}
  {# {% include 'components/hero-slider/hero-slider.html.twig' %} #}

  <div class="container-fluid">
    <div class="container">
        <!-- Component -->
        <div id="product-configuration-section" class="col-12 p-0">
            <div class="row p-0">
              <div class="col-12 p-3">
                <h6 class="mini-header">Configurador</h6>
                <h2 class="text-header">¿Qué tipo de seguro está buscando?</h2>
                <p>Descubre el seguro que mejor se adapta a tus necesidades</p>
              </div>
            </div>
          <div class="col-12 p-0">
            {% include 'components/product-selector/product-selector.html.twig' with {
              mainelements: mainelements,
              firstchildrens: firstchildrens,
              secondChildrens: secondChildrens
            }
            %}
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="fullscreen-product-config">
    <div class="close-button">
      <span class="icon-cancel-circle h2 m-0"></span>
    </div>
    <div class="product-config container">
      <h1>Configurador</h1>
      <p>Este es el configurador de producto a toda pantalla</p>
    </div>
  </div>

{% endblock %}
